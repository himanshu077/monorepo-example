{
  "name": "@demo/monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "build-components": "cd packages && cd components && yarn build",
    "watch-components": "cd packages && cd components && yarn build-watch",
    "start": "cd app && yarn start",
    "build": "cd app && yarn build"
  }
}